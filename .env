# Database Configuration
# Option 1: Individual Parameters (Recommended - handles special characters in passwords)
DB_HOST=localhost
DB_PORT=5432

DB_USER=postgres
DB_PASSWORD=16Sep1998
DB_NAME=central360

# Alternative: Option 2 - Connection String (uncomment and use this if preferred)
# DATABASE_URL=postgres://username:password@localhost:5432/central360
# Note: If using DATABASE_URL and password has special characters, URL-encode them:
# @ -> %40, : -> %3A, / -> %2F, # -> %23, % -> %25, etc.

# JWT Configuration
JWT_SECRET=passwordismybirthday
JWT_EXPIRES_IN=1d

# Server Port (optional, defaults to 4000)
PORT=4000
